import React, { useEffect } from 'react';
import * as XLSX from 'xlsx';

export const Enhanched = () => {

    useEffect(() => {
        // XLSX.readFile('./test.xlsx', { type: 'binary' })

    }, [])

    return (
        <div>
            <h1>Enhanched</h1>
        </div>
    );
};

